node_modules/
dist/
build/
.env
.env.local
*.log
.DS_Store
.expo/
ios/
android/
*.orig.*
web-build/
.claude/
.superpowers/
coverage/
logs/

# Mobile secrets — never commit
# NOTE: google-services.json / GoogleService-Info.plist are NOT secret (they ship
# inside the app binary) and MUST stay tracked so EAS Build can upload them.
google-play-service-account*.json
*.keystore
*.p8


# 데모 시드 스크립트 — 무방비 deleteMany(감사로그 포함) 및 테넌트 이동 위험이 있어
# 저장소에 두지 않는다. 하드닝 전까지 로컬 전용. (backlog D8)
packages/backend/seed-demo*.js
